---
agent: webo
task: Error boundaries + URL-based pagination di table components
date: 2026-07-02
status: approved
---

## Task
Tambah error boundaries Next.js App Router + wire pagination component ke clients table dan pipeline list view.

## Steps That Worked
1. `src/app/(dashboard)/error.tsx` — client component, Card + Button "Coba lagi" via `reset()`, tampilkan `error.digest`.
2. `src/app/global-error.tsx` — wajib render `<html><body>` sendiri; TIDAK bisa pakai shadcn (provider belum mount) → inline CSS mengikuti design tokens globals.css.
3. Pagination client-side 25/page, page state di URL `?page=` (shareable), reset otomatis saat sort/search/filter berubah.

## Pitfalls
- **Footer stats (count/sum/avg/median) harus dihitung dari full filtered set, BUKAN dari paged slice** — kalau dari page, angka "total" menyesatkan. (Ini sempat salah, direvisi.)
- Select-all checkbox scope-nya current page saja — itu benar.
- Jangan ubah API routes kalau data sudah in-memory di client — client-side pagination dulu.

## Verification
- Build 24/24 pages, footer stats konsisten sebelum/sesudah pagination.
