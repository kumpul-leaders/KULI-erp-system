---
agent: webo
task: Test harness minimal — Vitest + Playwright smoke di Next.js app tanpa test sebelumnya
date: 2026-07-02
status: approved
---

## Task
Pasang test harness minimal (bukan coverage penuh): unit test untuk Zod validation schemas + E2E smoke tanpa kredensial login.

## Steps That Worked
1. Vitest: environment `node` (schema test tak butuh DOM), alias `@/` via `vite-tsconfig-paths`, scope `src/**/__tests__/**/*.test.ts` supaya tidak menyentuh app code Next.js.
2. 57 assertion, 4 file: valid payload lolos, enum invalid ditolak, optional absent OK, cross-field (periodMonth vs type), unknown keys di-strip.
3. Playwright: `webServer` dgn `reuseExistingServer: true`; smoke tanpa auth — login form render, `/dashboard` tanpa auth → redirect `/login`, `/forgot-password` render.
4. Test yang butuh login (`create lead`, `drag stage`) = `test.skip` + komentar cara enable (PLAYWRIGHT_EMAIL/PASSWORD + auth fixture) — bukan dihilangkan.
5. npm scripts: `test`, `test:watch`, `test:e2e`.

## Pitfalls
- `e2e/` harus di-exclude dari `tsconfig.json` supaya `@playwright/test` imports tidak masuk kompilasi Next.js build.
- `test-results/` + `playwright-report/` masuk `.gitignore`.

## Verification
- `npm run test` 57/57 pass; `npm run test:e2e` 3 pass 2 skip; `npm run build` tetap clean.
